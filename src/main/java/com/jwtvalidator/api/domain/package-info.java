/**
 * Conceitos centrais do dominio: estrutura de um JWT (JwtStructureResult),
 * validador estrutural (JwtStructuralValidator) e os subpacotes "rule"
 * (regras de negocio sobre as claims) e "util" (utilitarios de apoio).
 * Este pacote nao conhece HTTP, nem framework - apenas regras e conceitos.
 */
package com.jwtvalidator.api.domain;
