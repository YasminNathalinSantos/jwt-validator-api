/**
 * Contém as regras de negócio puras de validação do JWT
 * (Name, Role, Seed) e a validação estrutural do token.
 * Este pacote não conhece HTTP, nem framework — apenas regras.
 */
package com.jwtvalidator.api.domain;
